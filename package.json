{
	"name": "resumejson_claude",
	"version": "0.1.0",
	"description": "Convert resumes between PDF/JSON/Markdown and run an AI-powered ATS pipeline — driven by Claude skills on your Claude subscription, no metered API key.",
	"type": "module",
	"bin": {
		"resumejson_claude": "dist/cli.js"
	},
	"main": "dist/cli.js",
	"types": "dist/cli.d.ts",
	"files": [
		"dist",
		"dotclaude_folder",
		"LICENSE"
	],
	"engines": {
		"node": ">=20.12"
	},
	"scripts": {
		"resumejson_claude": "tsx src/cli.ts",
		"install:dotclaude": "tsx src/cli.ts install .claude --mode symlink",
		"typecheck": "tsc --noEmit",
		"test": "node --import tsx --test 'tests/**/*.test.ts'",
		"test:watch": "node --import tsx --test --watch 'tests/**/*.test.ts'",
		"build": "tsc -p tsconfig.build.json",
		"publish:all": "npm run build && npm version patch && npm publish"
	},
	"author": {
		"name": "Jerome Etienne",
		"email": "jerome.etienne@gmail.com"
	},
	"license": "MIT",
	"keywords": [
		"claude-code",
		"resume",
		"json-resume",
		"ats",
		"skill"
	],
	"dependencies": {
		"chalk": "^5.3.0",
		"commander": "^12.1.0",
		"zod": "^3.23.8",
		"zod-to-json-schema": "^3.23.5"
	},
	"devDependencies": {
		"@types/node": "^22.10.0",
		"tsx": "^4.19.2",
		"typescript": "^5.7.2"
	}
}
